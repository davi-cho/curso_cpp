#include <iostream>

using namespace std;

int main()
{
    // bool b = false;
    // cout << "el valor de b es " << b << " su tamaño es " << sizeof(b) << endl;

    // char c = 65;  // 'A'
    // cout << "el valor de c es " << c << " su tamaño es " << sizeof(c) << endl;

    wchar_t wc = 269;
    cout << "el valor de wc es " << wc << " su tamaño es " << sizeof(wc) << endl;
    getchar();
    return 0;
}